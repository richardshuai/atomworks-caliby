This folder contains setup details that are specific to the IPD.

To run the IPD setup, simply execute the `setup.sh` script with source from the root of the repository:
```bash
source ./.ipd/setup.sh
```

This will add the datahub module to your PYTHONPATH and set the CCD_MIRROR_PATH and PDB_MIRROR_PATH environment variables.
